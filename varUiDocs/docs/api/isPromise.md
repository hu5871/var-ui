# 是否是Promise

使用方法：

```js
const isPromise=uni.$var.utils.isPromise(new Promise())
```

参数：

```typescript
/**
 * @param {any} value
 * @return boolean
 */
export function isPromise(value: any)
```



